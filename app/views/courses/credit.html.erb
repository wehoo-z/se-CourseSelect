<div class="container-fluid">
  <div class="row">
    <div class="col-sm-2">
      <%= render "shared/sidenav" %>
    </div>
    <div class="col-sm-10">
      <div class="panel panel-primary">
        <div class="panel-heading">
          <h3 class="panel-title">学分统计</h3>
        </div>
        <div class="panel-body" style="min-height: 450px">
         <p>满足学习要求是以获取学分是否满足学习要求为判断依据。</p>
         <table class="table table-striped table-hover">
          <thead>
           <tr>
             <th>学习要求</th>
             <th>已选课程</th>
             <th>已选学分</th>
             <th>已得学分</th>
           </tr>
         </thead>
         <tbody>
          <tr>
            <td rowspan=<%= @row+2 %>> 
              &gt;=<%= @scoreNeed %>
            </td>
            <td>
            </td>
            <td rowspan=<%= @row+2 %>>
              <%= @scoreSelect %>
            </td>
            <td rowspan=<%= @row+2 %>>
              <%= @scoreGot %>
            </td>
          </tr>

          <% @courses.each do |course| %>
          <tr>
            <td>
              <%= course.name+"("+course.credit+")" %>
            </td>
          </tr>
          
          <% end %>
          <tr>
           <td></td>
          </tr>
        
        </tbody>
      </table>
    </div>
  </div>
</div>
</div>
</div>

